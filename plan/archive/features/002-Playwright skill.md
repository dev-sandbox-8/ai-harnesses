# Skill for creating playwright tests

## Description

## Repro steps

## Acceptance criteria
  - Make sure the right method (e.g. post vs. fetch) is used for API requests
  - Do not use default values (i.e. those shown in the UI by default) for playwright tests, as this can hide special cases and lead to false positives
  - Favour manual actions over mocks and network calls

## Open questions